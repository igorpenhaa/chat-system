apiVersion: v1
kind: Service
metadata:
  name: central
  namespace: chat-system
spec:
  selector:
    app: central
  ports:
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051

