apiVersion: v1
kind: Service
metadata:
  name: central
  namespace: chat-system
spec:
  selector:
    app: central
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32080

