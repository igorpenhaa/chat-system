apiVersion: v1
kind: Service
metadata:
  name: serverb
  namespace: chat-system
spec:
  selector:
    app: serverb
  ports:
    - name: grpc
      protocol: TCP
      port: 50053
      targetPort: 50053

