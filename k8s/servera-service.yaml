apiVersion: v1
kind: Service
metadata:
  name: servera
  namespace: chat-system
spec:
  selector:
    app: servera
  ports:
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051
  type: ClusterIP
