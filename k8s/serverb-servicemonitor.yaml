apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: serverb-monitor
  namespace: chat-system
  labels:
    release: kube-prom
spec:
  selector:
    matchLabels:
      app: serverb
  namespaceSelector:
    matchNames:
    - chat-system
  endpoints:
  - port: metrics
    path: /metrics
    interval: 15s
    scheme: http
