apiVersion: apps/v1
kind: Deployment
metadata:
  name: servera
  namespace: chat-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servera
  template:
    metadata:
      labels:
        app: servera
    spec:
      containers:
      - name: servera
        image: servera:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 50052
        - containerPort: 8000
