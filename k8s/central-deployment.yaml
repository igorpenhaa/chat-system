apiVersion: apps/v1
kind: Deployment
metadata:
  name: central
  namespace: chat-system
spec:
  selector:
    matchLabels:
      app: central
  template:
    metadata:
      labels:
        app: central
        prometheus: "true"
    spec:
      containers:
        - name: central
          image: central:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
            - containerPort: 8000
          env:
            - name: SERVER_A_ADDRESS
              value: "servera.chat-system.svc.cluster.local:50051"
            - name: SERVER_B_ADDRESS
              value: "serverb.chat-system.svc.cluster.local:50052"
